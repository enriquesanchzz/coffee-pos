// Sucursal única hasta Fase 5 (multi-sucursal). El id coincide con el que
// siembra `prisma/seed.ts` (`Branch` + `StockLocation` iniciales).
export const DEFAULT_BRANCH_ID = "branch-principal";
export const DEFAULT_STOCK_LOCATION_ID = "stock-branch-principal";
